# Result

This example shows Forge handling a straightforward product request without letting the agent skip permission analysis or review.

The shareable takeaway:

- `prompt.md` clarifies scope before code
- `research.md` ties the feature to existing query and guard patterns
- `plan.md` is concrete enough to implement directly
- `review.md` catches a real gap before the task is considered done
