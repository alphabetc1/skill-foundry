# Request

Add CSV and JSON export for invoices in the admin dashboard.

Requirements:

- Reuse the existing invoice filters
- Support large exports without timing out
- Limit access to finance admins
- Log who triggered each export
- Add tests for permissions and format correctness
